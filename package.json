{
  "name": "thekingwedding",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "deploy": "node scripts/list-invite-bg.mjs && wrangler pages deploy . --project-name=thekingwedding"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
