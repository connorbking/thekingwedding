{
  "name": "thekingwedding",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev . --persist-to=./.wrangler/state",
    "deploy": "wrangler pages deploy . --project-name=thekingwedding"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}
