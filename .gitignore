.DS_Store
Thumbs.db
*.log
node_modules
.wrangler
.dev.vars
.seed/
.vercel
.idea

# Local guest list only — never commit the sheet or the reader
local/
*.xlsx
*.xls

# Source model is over GitHub's 100 MB limit
public/key.obj
